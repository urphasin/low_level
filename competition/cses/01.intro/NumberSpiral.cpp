#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int solve(int y, int x) {

    return 0;
}

int main() {
    printf("Golden ratio: \x1b[95m%f\n\n\x1b[0m", (1/2 + sqrt(5)/2));
    int t; cin >> t;
    
    while(t--) {
        int y, x; cin >> y >> x;

        solve(y, x);
    }
}